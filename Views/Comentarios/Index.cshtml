@model IEnumerable<adventureworks.Models.comentario>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.comentario_subject)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.comentario_body)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.foto.foto_titulo)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.usuario.usuario_nombre)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.comentario_subject)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.comentario_body)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.foto.foto_titulo)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.usuario.usuario_nombre)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.comentario_id }) |
            @Html.ActionLink("Details", "Details", new { id=item.comentario_id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.comentario_id })
        </td>
    </tr>
}

</table>
